//P1217 [USACO1.5]回文质数 Prime Palindromes

#include<stdio.h>
#include<math.h>
int hw(int q);
int zs(int q);

main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    for(;a<=b;a++)
    {
        if(hw(a)&&zs(a))
            printf("%d\n",a);
    }
    return 0;
}

int hw(int q)
{
    int i,e,f,g=1,h,k,l;
    l=q;
    for(i=1;;i++)
    {
        l=l/10;
        if(l==0)
            break;
    }
    for(e=1,f=i-1;e<=i/2;e++,f--)
            {
                h=(q/((e-1)*10))%10;
                k=(q/(f*10))%10;
                if(h!=k)
                    {g=0;
                    break;}
            }
    return g;
}

int zs(int q)
{
    int i,g=1;
    for(i=2;i<=floor(sqrt(q));i++)
    {
        if(q%i==0)
        {g=0;
        break;}
    }
    return g;
}
